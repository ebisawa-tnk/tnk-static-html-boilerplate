{
  "name": "tnk-static-html-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:sass": "sass --no-source-map src/scss/:dist/css/",
    "watch:sass": "sass --no-source-map --watch src/scss/:dist/css/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.62.0"
  }
}
